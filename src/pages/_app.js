import '../styles/FAQ.module.css'; // Import global styles if needed
import '/public/assets/css/marca.css'; // Your existing CSS file

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;